name: Accessibility Gate

on:
  pull_request:
    paths:
      - 'templates/**'
      - 'static/css/**'
      - 'app/routes/**'
      - 'app/forms.py'
      - 'tests/test_accessibility.py'
      - 'tests/test_axe_compliance.py'

permissions:
  contents: read
  pull-requests: write

jobs:
  accessibility-audit:
    name: WCAG 2.1 AA Compliance Audit
    runs-on: ubuntu-latest
    
    # Use a service container for the test database
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
          POSTGRES_DB: classroom_test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-playwright
          playwright install chromium --with-deps

      - name: Initialize Database
        env:
          FLASK_APP: app
          FLASK_ENV: testing
          DATABASE_URL: postgresql://user:password@localhost:5432/classroom_test_db
        run: |
          flask db upgrade

      - name: Run Accessibility Test Suites
        env:
          FLASK_APP: app
          FLASK_ENV: testing
          DATABASE_URL: postgresql://user:password@localhost:5432/classroom_test_db
          
          # Testing-only secrets — DO NOT reuse in production
          ENCRYPTION_KEY: UENU-3IwrMOdyGCaQR-2suY5Mv5NCsyQkja6CHJBG1I=
          PEPPER_KEY: 9183723b5d7ba66102f766a6a8600a76fe4b95b9be57fb7d8fd1791b67ecdf7c
          SECRET_KEY: 7536c8660c96747163e93cefd072489243ba976abd646cb3df9e26d2af3e2afe
        run: |
          echo "♿ Running Structural & Axe-Core Accessibility Audits..."
          pytest tests/test_accessibility.py tests/test_axe_compliance.py -v
