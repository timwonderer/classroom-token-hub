name: Deploy status service to Google Cloud

on:
  push:
    branches: [main]
    paths:
      - "status/**"
      - "status_service/**"
      - "tests/test_status_*.py"
      - "tests/dom/operation/test_health.py"
      - ".github/workflows/deploy-status.yml"
  pull_request:
    paths:
      - "status/**"
      - "status_service/**"
      - "tests/test_status_*.py"
      - "tests/dom/operation/test_health.py"
      - ".github/workflows/deploy-status.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: cth-production-status
  GCP_REGION: us-west2
  ARTIFACT_REPOSITORY: status
  IMAGE_NAME: cth-status

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: classroom_economy_v2_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d classroom_economy_v2_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install status dependencies
        run: python -m pip install -r requirements.txt -r status_service/requirements.txt pytest
      - name: Compile status service
        run: python -m compileall -q status status_service
      - name: Run focused status tests
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/classroom_economy_v2_test
        run: pytest -q tests/test_status_contracts.py tests/test_status_projection.py tests/test_status_telemetry.py

  deploy:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOY_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
      - name: Authenticate Artifact Registry
        run: gcloud auth configure-docker "${GCP_REGION}-docker.pkg.dev" --quiet
      - name: Build and push status image
        env:
          IMAGE_URI: ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.ARTIFACT_REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        run: |
          docker build -t "$IMAGE_URI" status_service
          docker push "$IMAGE_URI"
          echo "IMAGE_URI=$IMAGE_URI" >> "$GITHUB_ENV"
      - name: Deploy public status service
        run: >-
          gcloud run deploy cth-status-public
          --image "$IMAGE_URI"
          --region "$GCP_REGION"
          --project "$GCP_PROJECT_ID"
          --set-env-vars "STATUS_SERVICE_MODE=public,FIRESTORE_DATABASE=cth-status-prod"
          --quiet
      - name: Deploy operator status service
        run: >-
          gcloud run deploy cth-status-operator
          --image "$IMAGE_URI"
          --region "$GCP_REGION"
          --project "$GCP_PROJECT_ID"
          --set-env-vars "STATUS_SERVICE_MODE=operator,FIRESTORE_DATABASE=cth-status-prod"
          --set-secrets "STATUS_SESSION_SECRET=status-session-secret:latest"
          --quiet
      - name: Verify public status signal endpoint
        run: |
          URL="$(gcloud run services describe cth-status-public --region "$GCP_REGION" --project "$GCP_PROJECT_ID" --format='value(status.url)')/health/status"
          curl --fail --silent --show-error "$URL"
