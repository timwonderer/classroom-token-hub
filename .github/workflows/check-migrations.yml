name: Check Database Migrations

# Both legs previously filtered on `codex/v2.0`, a branch that exists neither
# locally nor on the remote, so this gate was entirely inert — no migration was
# ever validated by CI. The PR leg now matches any target branch: migrations are
# this repo's most documented source of deploy failure, and a validation gate
# must not be able to go silently dead again the next time a branch is renamed.
#
# `app/models/**` never matched either: this repo has `app/models.py`, a module,
# not a package. Both spellings are listed so the filter survives a future split.
on:
  push:
    branches: [ main ]
    paths:
      - 'migrations/versions/**'
      - 'migrations/lint_baseline.txt'
      - 'app/models.py'
      - 'app/models/**'
      - 'scripts/validate-migrations.py'
      - 'scripts/lint_migrations.py'
  pull_request:
    branches: [ '**' ]
    paths:
      - 'migrations/versions/**'
      - 'migrations/lint_baseline.txt'
      - 'app/models.py'
      - 'app/models/**'
      - 'scripts/validate-migrations.py'
      - 'scripts/lint_migrations.py'

  # Also allow manual trigger
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write  # For commenting on PRs

jobs:
  validate-migrations:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Full history needed for migration checks

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v6
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install alembic flask-migrate
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Migration Validation Script
        run: |
          echo "🔍 Running comprehensive migration validation..."
          python scripts/validate-migrations.py

      # SOP-DB-011 has required the linter since 2026-02-04, but nothing ran it,
      # so migrations dated 2026-09 landed non-idempotent. The whole corpus is
      # linted against a frozen baseline of the debt SOP-DB-009 VI already
      # accepts: history may fail, new work may not, and the baseline can only
      # shrink. Editing merged migrations to clear it is forbidden by policy.
      #
      # On a pull request the baseline is also compared against the base branch's
      # copy. Reading it only from the PR's own checkout let a branch excuse its
      # own failing migration by appending one line to the very file the gate trusts.
      - name: Lint migrations for idempotency
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          echo "🔍 Linting migrations against the frozen baseline..."
          COMPARE=""
          if [ -n "$BASE_REF" ]; then
            if git show "origin/$BASE_REF:migrations/lint_baseline.txt" > /tmp/base_branch_baseline.txt 2>/dev/null; then
              echo "   Comparing baseline against origin/$BASE_REF."
              COMPARE="--base-branch-baseline /tmp/base_branch_baseline.txt"
            else
              # The branch that introduces the baseline is the one branch whose
              # entries are all legitimately new, so there is nothing to compare
              # against yet. Treating an absent file as an empty prior baseline
              # rejects exactly that branch and no other.
              echo "   No baseline on origin/$BASE_REF yet; additions cannot be compared."
            fi
          fi
          python scripts/lint_migrations.py --baseline migrations/lint_baseline.txt $COMPARE

      - name: Comment on PR (if check fails)
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const runId = context.runId;

            const jobs = await github.rest.actions.listJobsForWorkflowRun({
              owner,
              repo,
              run_id: runId,
              per_page: 100
            });

            const failedConclusions = new Set(['failure', 'timed_out', 'cancelled', 'startup_failure', 'action_required']);
            const allJobs = jobs.data.jobs || [];

            // Prefer selecting the migration job by name and completion status,
            // with fallbacks when conclusions may still be null.
            let migrationJob =
              allJobs.find(
                (job) =>
                  typeof job.name === 'string' &&
                  job.name.toLowerCase().includes('validate-migrations') &&
                  job.status === 'completed'
              ) || null;

            if (!migrationJob) {
              const failedJobs = allJobs.filter((job) => failedConclusions.has(job.conclusion));
              migrationJob =
                failedJobs.find(
                  (job) =>
                    typeof job.name === 'string' &&
                    job.name.toLowerCase().includes('validate-migrations')
                ) ||
                failedJobs[0] ||
                allJobs.find(
                  (job) =>
                    typeof job.name === 'string' &&
                    job.name.toLowerCase().includes('validate-migrations')
                ) ||
                allJobs[0] ||
                null;
            }
            const failedSteps = (migrationJob?.steps || [])
              .filter((step) => failedConclusions.has(step.conclusion))
              .map((step) => `- ${step.name} (${step.conclusion})`);

            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${runId}`;
            const jobUrl = migrationJob?.html_url || runUrl;
            const failureSection = failedSteps.length
              ? failedSteps.join('\n')
              : '- Failure details unavailable from job metadata; open logs link below.';

            const marker = '<!-- migration-validation-failure-comment -->';
            const body = `${marker}\n⚠️ **Migration Validation Failed**\n\nThe migration validation workflow failed.\n\n**Detected failure context**\n- Job conclusion: \`${migrationJob?.conclusion || 'failure'}\`\n- Failed step(s):\n${failureSection}\n\n**What this workflow validates**\n1. Continuous revision chain (no breaks/gaps)\n2. Single migration head\n3. Correct \`upgrade()\` and \`downgrade()\` functions\n4. Idempotency guards (for example around \`add_column\`)\n\n**Logs**\n- [Failed job logs](${jobUrl})\n- [Workflow run](${runUrl})`;

            const comments = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number: context.issue.number,
              per_page: 100
            });
            const existing = comments.data.find((comment) => {
              const isBot = comment.user?.type === 'Bot' || comment.user?.login?.includes('[bot]');
              return isBot && typeof comment.body === 'string' && comment.body.includes(marker);
            });

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner,
                repo,
                body
              });
            }
