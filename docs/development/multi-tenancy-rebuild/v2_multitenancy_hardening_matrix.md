# v2 Multitenancy Hardening Matrix

Date: 2026-02-16  
Branch: `join-code-centric-architecture-rebuild`

| Workstream | Must Be True Before v2 | Status | Current Coverage / Evidence | Corresponding Tests |
|---|---|---|---|---|
| Core tenancy boundary | `join_code` is the primary isolation boundary in data model and routing | Complete | `ClassEconomy`, `ClassMembership`, `ClassJoinCodeAlias` live and in use; tracked in `docs/development/multi-tenancy-rebuild/progress_log.md` | `tests/test_class_context_and_switching.py`, `tests/test_join_code_generation.py` |
| Membership auth primitives | Class actions require membership validation (`check_membership_access`/`membership_required`) | Partial | Implemented in auth and added in multiple high-risk admin routes; still not universal across all class-scoped endpoints | `tests/test_admin_membership_gates.py`, `tests/test_admin_tenancy.py` |
| Purchase authorization isolation | No cross-tenant spend; purchase must use scoped balances | Complete | `/api/purchase-item` uses join-code scoped balances; no global fallback | `tests/test_collective_goal_progress.py`, `tests/test_transfer_legacy_transactions.py`, `tests/test_void_transaction_rules.py` |
| CSV export isolation | Teacher export must include only teacher-owned join-code financials | Complete | `/admin/export-students` now aggregates via teacher-owned active class memberships | `tests/test_admin_export_students_scoping.py` |
| Admin dashboard/banking aggregates | No global student financial properties in class-scoped admin stats | Partial (major high-risk parts fixed) | Dashboard + banking summary moved to scoped helper; remaining codebase still has legacy global properties for compatibility | `tests/test_admin_export_students_scoping.py`, `tests/test_admin_multi_tenancy.py` |
| Admin class selection + deletion gating | Switch/delete class routes must require active admin membership | Complete (for targeted routes) | `/admin/current-class`, `/admin/students/delete-block`, `/admin/join-code/delete` gated; legacy bootstrap guarded for migration | `tests/test_admin_membership_gates.py`, `tests/test_legacy_bulk_deletion.py`, `tests/test_legacy_unclaimed_deletion.py` |
| Issue queue scoping | Issue views/actions must respect selected authorized join_code | Partial | `/admin/issues` now uses `current_join_code` + owned join-codes; broader issue subsystem still needs full gate audit | `tests/test_admin_membership_gates.py`, `tests/test_issue_resolution_reverse_transaction.py` |
| Issue reverse action safety | Reverse must validate scope + produce ledger-safe reversal | Complete (for this path) | `/admin/issues/<id>/resolve` now checks `student_id/teacher_id/join_code` and creates reversal for posted tx | `tests/test_issue_resolution_reverse_transaction.py` |
| Immutable ledger semantics | No mutating historical records as primary void mechanism | Partial | Many void flows now create reversals; `is_void` mutation still exists and full immutable semantics not complete | `tests/test_void_transaction_rules.py`, `tests/test_banking_core.py` |
| Float/precision hardening | Currency logic should avoid float drift in financial invariants | Partial | Core money storage is `Numeric`; balance cache uses integer cents; float conversions still exist in presentation/compat paths | `tests/test_decimal_precision.py`, `tests/test_banking_core.py` |
| Query inversion completion | Remove remaining class scoping based on `teacher_id/block` and legacy `join_code IS NULL` blending | Pending/Partial | High-risk paths fixed; full sweep still outstanding in admin/api/student/system_admin | Needs additional route-level regression additions in `tests/` |
| “All sections” semantics | “Apply to all” must mean explicit fan-out over teacher-owned join-codes only | Partial | Pattern implemented in several admin financial paths; needs comprehensive endpoint-by-endpoint confirmation | Existing tenancy tests partly cover; needs dedicated all-sections suite expansion |
| Audit anchor completeness | All state-changing writes must persist `actor_membership_id` | Partial | Hall pass anchor fixed; many transaction paths set anchor; not yet universal | `tests/test_redemption_audit_log.py` plus new dedicated actor-anchor tests needed |
| Legacy bypass removal | Deprecate/remove routes/branches that allow unscoped class actions | Pending | Migration compatibility code still present by design | Needs dedicated deny-path tests for every deprecated/unscoped branch |
| Join-code delete semantics | Deleting join_code destroys all data in that scope | Accepted intended behavior | Explicitly affirmed in risk report and progress log; behavior implemented | `tests/test_join_code_deletion_semantics.py`, `tests/test_teacher_block_cleanup.py` |
| Destructive UX safeguards | Multi-step destructive confirmation flow before hard deletion | Pending | Not yet implemented | No test coverage yet |
