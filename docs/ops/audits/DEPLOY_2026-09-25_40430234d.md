# Deployment record — 2026-09-25 redeploy

**Type:** Redeployment onto the existing live-test host, continuing the §XI
campaign. Governed by `SOP-DEP-001`. See *Deviations* below.

## Release identity

| | |
|---|---|
| Release SHA | `40430234d9d829737c9737c6f2b6786ae784fed3` |
| Deployable ref | tag `live-test/2026-09-25a` (annotated, pushed) |
| Branch | `main` |
| Previous deployed SHA | `db1cddec5` (tag `live-test/2026-09-23i`) |
| Operator | timothy.cs.chang@gmail.com (deploy executed by Claude at operator instruction) |
| Verifier | solo-operator exception, §XIII |

Tag verified: `git rev-parse live-test/2026-09-25a^{commit}` equals the release SHA and `HEAD` on `main`.

**Contents since `db1cddec5`:** obligation-state derivation and `WITHDRAWN` / advance assessment / current
cycle / termination instant (DOM-OBL-001 v3.2); rent conformance to advance billing and disablement;
insurance recurring premiums (FEAT-STOR-007: entitlement-keyed premium lineage, renewal executor and hourly
job, nonpayment modes, per-period claims); student payment of an overdue premium; status-page links on the
public site, login pages and help pages.

## §VI Release and Authority Gate

| Step | Result |
|---|---|
| VI.1 commit on `main`, SHA recorded | PASS |
| VI.2 working tree clean at the SHA | PASS |
| VI.3 single migration head | PASS — `d7a3e9b1c5f2` |
| VI.4 PostgreSQL-backed suite | See deviation 1 |

## §VII Runtime Preparation

- Service stopped, then checkout replaced with the **tag**, detached: `git checkout --detach live-test/2026-09-25a`;
  `git clean -fd docs/`. `HEAD` verified equal to the release SHA.
- `requirements.txt` unchanged since `db1cddec5`; `pip install --dry-run` reports nothing to install.

## §IX Migration

**§XIV.6 snapshot** taken before the upgrade: `/root/backups/pre-d7a3e9b1c5f2_20260925T122616Z.sql`
(410K, mode 600), 45 `COPY` blocks present.

`flask db upgrade` ran `b8e1f4c2a5d9 → c4d6e8f0a2b3 → d7a3e9b1c5f2`: created `obligation_command_reservation`
and its two indexes; added `bill_preview_days`, `nonpayment_mode`, `cancel_after_days` to `insurance_policies`.
`flask db current` afterwards: `d7a3e9b1c5f2 (head)`.

## §X Application Start and Health Checks

| Check | Result |
|---|---|
| X.2 `systemctl is-active` | `active` |
| X.3 boot log | no tracebacks or unhandled exceptions |
| X.4 `/health` (gunicorn, `127.0.0.1:8000`) | HTTP 200, body `ok` |
| X.5 `/health/status` | HTTP 200 |
| Route smoke | `/student/login`, `/admin/login`, `/docs/` 200; authenticated pages 302 to login; no 500 |

Port 80 answers `/health` with nginx's 301 to HTTPS, and the public host answers 302 (Cloudflare Access);
both expected.

## Deviations

1. **Suite run against a predecessor SHA.** The full suite ran at `ec841e7ee` (3659 passed, 17 skipped;
   10 failed / 20 errors, all environmental or non-code: missing Playwright driver, missing `google-auth`,
   a pending doc rename, a status-template string, and a pre-slice-4 fixture claim). Each was fixed and its
   files rerun green. The commits after `ec841e7ee` touch docs, tests, templates and one status template
   only; their targeted tests passed (`tests/simulated` 5/5, `tests/test_status_page.py` 16/16, docs index
   688/688, accessibility pages 2/2). No application Python changed after the full run.
2. **Push bypassed the pull-request rule** on `main` (repository-admin bypass), as on prior live-test deploys.

## Pre-existing log noise (not introduced by this release)

- `/metrics` is polled every 15 s against a 500/day rate limit, producing a steady 429 + WARNING stream
  (460 in the two hours before deploy).
- `/health` and `/health/status` log `TLCP-INVARIANT-VIOLATION: missing canonical context` on every call
  (170 in the three hours before deploy): neither endpoint is in TLCP's public or no-context allowlist.
  Same class as live-test finding 67; alarm-fatigue noise that could mask a real violation.

## Still to exercise live

Insurance purchase, failed autopay then manual premium payment, stop-renewal, teacher claim review, and
disabling rent with debt outstanding. Rent payment waits for the 2026-09-29 preview window.
