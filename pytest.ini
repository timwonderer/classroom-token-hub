[pytest]
markers =
    critical: Critical validation paths (e.g., Claim, Transfer).
    db_full_reset: Rebuild the Postgres test database for contamination-sensitive tests.
    regression: Regression tests for fixed bugs.
    slow: Slow running tests.
